export const BASE_API_URL = 'https://hacker-news.firebaseio.com/v0'

export const UPDATE_TIMEOUT = 60000

export const appRoutesNames = Object.freeze({
    HOME: "/home",
    STORIES: "/stories"
})

export const STORIES_COUNT = 20
